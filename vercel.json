{
  "version": 2,
  "buildCommand": "cd user-guard-verse-main && npm install && npm run build && cd .. && cp -r user-guard-verse-main/dist ./public",
  "outputDirectory": "public",
  "builds": [
    {
      "src": "api/**/*.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
